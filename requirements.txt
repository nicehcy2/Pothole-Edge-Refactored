# PyTorch - GPU 사용 시 아래 명령어로 별도 설치 (CUDA 버전은 본인 환경에 맞게)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU 전용은 ultralytics 의존성으로 자동 설치됨

# YOLOv8
ultralytics==8.2.83

# image processing
opencv-python
numpy

# GPS (FlaskGPSProvider)
requests

# GPS (LTEGPSProvider, HardwareGPSProvider)
pyserial

# geohash
pygeohash

# S3
boto3
