# PyTorch - For GPU support, install separately with CUDA version:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# CPU-only version is installed automatically via ultralytics dependency

# YOLOv8
ultralytics==8.2.83

# image processing
opencv-python
numpy

# GPS (FlaskGPSProvider)
requests

# GPS (LTEGPSProvider, HardwareGPSProvider)
pyserial

# geohash
pygeohash

# S3
boto3

# env
python-dotenv
